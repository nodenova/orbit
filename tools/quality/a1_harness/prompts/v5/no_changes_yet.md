Working tree at turn {turns_used} of {max_turns}: `git status` reports no modified files
and no new ones. Nothing you have read or planned so far has been written down.

This task is graded on the diff. Reading that never reaches `edit_file` or `write_file`
scores the same as reading nothing. If you know which file to change, change it now and
refine it afterwards — a partial edit you improve beats a complete plan you never wrote.
