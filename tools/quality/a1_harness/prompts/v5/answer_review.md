Not recorded yet. This is your one revision, and the next `finish` is recorded exactly as
you write it. Check the answer you just wrote against three things:

1. **Exact names and numbers.** Every identifier, path, version, count and threshold in it
   must match the text you read, character for character. If a file you read states a
   quantity and your answer says "fewer", "a fraction" or "several", put the quantity back.
2. **Both halves.** The fact, and the mechanism of what goes wrong without it — what
   happens, in what order, and how it surfaces to whoever hits it. "It fails silently" is a
   label, not the mechanism.
3. **Grounding.** The file, and the line, where you read each claim.

If one of the three is missing and you are not certain of it, read the file again before you
answer. Keep the task's length and format limit. Then call `finish` with the final text —
unchanged if it already meets all three.
