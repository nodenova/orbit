The repository's own instruction file, `{path}`, is quoted below in full so you do not have
to go and find it. It is authoritative about this project's conventions, its rules about what
may cite what, and the traps it has already been caught by, and it carries the project's list
of invariants that must not be undone together with the reason for each — so when a question
asks which property or rule is load-bearing, that list is the authority.

Treat it as already read. It names concepts without always giving the answer, so still open
the files it points at, and quote the file you read rather than this summary of it.
