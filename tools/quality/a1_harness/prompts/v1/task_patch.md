{task_prompt}

The repository is at {worktree}. Edit the files there with `write_file` or `edit_file`.
Run the repository's own checks with `run` — `pytest -q`, `ruff check .`,
`ruff format --check .` and `mypy` — and fix what they report before you finish. When
you are done, call `finish` with one sentence saying what you changed and why.
